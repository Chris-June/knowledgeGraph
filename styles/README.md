# Styles

Global runtime styles currently live in `app/globals.css` for Next.js App Router compatibility.

This directory is reserved for shared design-token documentation or generated style artifacts that should remain separate from route-level CSS.
