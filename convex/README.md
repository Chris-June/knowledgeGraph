# Convex Contracts

This directory defines the contracts-first Convex boundary for the knowledge graph platform.

Live queries and mutations are intentionally deferred until the Convex project is linked and approved. The schema is present so graph persistence, audit events, AI memory, indexing, and RBAC work can be implemented without changing the platform shape.
